{%- style -%}
  #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.color_scheme.settings.background }};
    color: {{ section.settings.color_scheme.settings.text }};
  }

  .hero-banner-{{ section.id }} .hero-banner__container {
    max-width: var(--page-width);
    margin: 0 auto;
    padding: {{ section.settings.section_padding_top }}px {{ section.settings.section_padding_horizontal }}px {{ section.settings.section_padding_bottom }}px;
  }

  .hero-banner-{{ section.id }} .hero-banner__grid {
    display: flex;
    flex-direction: {% if section.settings.image_position == 'left' %}row{% else %}row-reverse{% endif %};
    align-items: center;
    gap: {{ section.settings.columns_gap }}px;
  }

  .hero-banner-{{ section.id }} .hero-banner__text-col {
    flex: 1;
    min-width: 0;
  }

  .hero-banner-{{ section.id }} .hero-banner__image-col {
    flex: 0 0 auto;
    width: {{ section.settings.image_width }}px;
    max-width: 100%;
  }

  .hero-banner-{{ section.id }} .hero-banner__title {
    margin-bottom: {{ section.settings.title_margin_bottom }}px;
  }

  .hero-banner-{{ section.id }} .hero-banner__subtitle {
    margin-bottom: {{ section.settings.subtitle_margin_bottom }}px;
    font-size: {{ section.settings.subtitle_size }}px;
  }

  .hero-banner-{{ section.id }} .hero-banner__description {
    margin-bottom: {{ section.settings.description_margin_bottom }}px;
  }

  /* Image wrapper — border overlay */
  .hero-banner-{{ section.id }} .hero-banner__image-wrapper {
    position: relative;
    display: block;
    width: 100%;
  }

  {%- if section.settings.enable_image_border -%}
  .hero-banner-{{ section.id }} .hero-banner__image-wrapper::after {
    content: '';
    position: absolute;
    inset: -0.5rem;
    border: {{ section.settings.border_thickness }}px solid {{ section.settings.decoration_color }};
    border-radius: {{ section.settings.decoration_border_radius }}px;
    pointer-events: none;
  }
  {%- endif -%}

  .hero-banner-{{ section.id }} .hero-banner__image img {
    width: {{ section.settings.image_width }}px;
    max-width: 100%;
    height: auto;
    display: block;
    border-radius: {% if section.settings.image_border_radius %}{{ section.settings.image_border_radius_px }}px{% else %}0{% endif %};
  }
  .hero-banner-{{ section.id }} .hero-banner__image {
    border-radius: {% if section.settings.image_border_radius %}{{ section.settings.image_border_radius_px }}px{% else %}0{% endif %};
    overflow: hidden;
  }

  /* Gallery layout */
  .hero-banner-{{ section.id }} .hero-banner__gallery {
    display: grid;
    gap: {{ section.settings.gallery_gap }}px;
    width: 100%;
  }

  {%- assign layout = section.settings.gallery_layout -%}
  {%- assign row1_h = section.settings.gallery_row1_height -%}
  {%- assign row2_h = section.settings.gallery_row2_height -%}

  {%- if layout == '1' -%}
    .hero-banner-{{ section.id }} .hero-banner__gallery {
      grid-template-columns: 1fr;
    }
  {%- elsif layout == '2-vertical' -%}
    .hero-banner-{{ section.id }} .hero-banner__gallery {
      grid-template-columns: 1fr;
      grid-template-rows: {{ row1_h }}px {{ row2_h }}px;
    }
  {%- elsif layout == '3-top1' -%}
    .hero-banner-{{ section.id }} .hero-banner__gallery {
      grid-template-columns: 1fr 1fr;
      grid-template-rows: {{ row1_h }}px {{ row2_h }}px;
    }
    .hero-banner-{{ section.id }} .hero-gallery__item--1 {
      grid-column: 1 / -1;
    }
  {%- elsif layout == '3-top2' -%}
    .hero-banner-{{ section.id }} .hero-banner__gallery {
      grid-template-columns: 1fr 1fr;
      grid-template-rows: {{ row1_h }}px {{ row2_h }}px;
    }
    .hero-banner-{{ section.id }} .hero-gallery__item--3 {
      grid-column: 1 / -1;
    }
  {%- elsif layout == '4-grid' -%}
    .hero-banner-{{ section.id }} .hero-banner__gallery {
      grid-template-columns: 1fr 1fr;
      grid-template-rows: {{ row1_h }}px {{ row2_h }}px;
    }
  {%- endif -%}

  .hero-banner-{{ section.id }} .hero-gallery__item {
    overflow: hidden;
    border-radius: {% if section.settings.image_border_radius %}{{ section.settings.image_border_radius_px }}px{% else %}0{% endif %};
    position: relative;
    min-height: 0; /* important for grid */
  }

  .hero-banner-{{ section.id }} .hero-gallery__item img,
  .hero-banner-{{ section.id }} .hero-gallery__item .placeholder-svg {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  {%- if section.settings.enable_image_border -%}
  .hero-banner-{{ section.id }} .hero-gallery__item::after {
    content: '';
    position: absolute;
    inset: 0;
    border: {{ section.settings.border_thickness }}px solid {{ section.settings.decoration_color }};
    border-radius: {% if section.settings.image_border_radius %}{{ section.settings.image_border_radius_px }}px{% else %}0{% endif %};
    pointer-events: none;
  }
  {%- endif -%}

  .hero-banner-{{ section.id }} .hero-banner__button {
    display: inline-block;
    background-color: {{ section.settings.button_bg_color }};
    color: {{ section.settings.button_text_color }};
    padding: {{ section.settings.button_padding_vertical }}px {{ section.settings.button_padding_horizontal }}px;
    border-radius: {{ section.settings.button_border_radius }}px;
    text-decoration: none;
    font-weight: 600;
    transition: opacity 0.2s ease;
  }

  .hero-banner-{{ section.id }} .hero-banner__button:hover {
    opacity: 0.85;
  }


  /* decor subtitle */
  {%- if section.settings.enable_subtitle_decor -%}
  #shopify-section-{{ section.id }} .hero-banner__subtitle {
    position: relative;
    display: inline-block;
    
  }

  #shopify-section-{{ section.id }} .hero-banner__subtitle::before {
    content: '';
    position: absolute;
    bottom: 18px;
    left:10%;
    width: 100%;
    height: 20px;
    background-image: url('https://cdn.shopify.com/s/files/1/0690/5433/3063/files/hero_curve.svg?v=1777451004');
    background-repeat: no-repeat;
    background-size: 100% 100%;
    pointer-events: none;
  }

  #shopify-section-{{ section.id }} .hero-banner__subtitle::after {
    content: '';
    display: inline-block;
    width: 0.75em;
    height: 0.75em;
    background-image: url('https://cdn.shopify.com/s/files/1/0690/5433/3063/files/hero_paw.svg?v=1777450948');
    background-repeat: no-repeat;
    background-size: contain;
    background-position: center;
    vertical-align: middle;
    left: 11.75rem;
    position: relative;
    top:-4.65rem;
  }
  {%- endif -%}


 @media screen and (max-width: 749px) {
    /* Контейнер секції */
    .hero-banner-{{ section.id }} .hero-banner__container {
      padding-top: {{ section.settings.section_padding_top_mobile }}px;
      padding-bottom: {{ section.settings.section_padding_bottom_mobile }}px;
      padding-left: {{ section.settings.section_padding_horizontal_mobile }}px;
      padding-right: {{ section.settings.section_padding_horizontal_mobile }}px;
    }

    /* Налаштування сітки */
    .hero-banner-{{ section.id }} .hero-banner__grid {
      flex-direction: column;
      gap: {{ section.settings.columns_gap_mobile }}px;
      align-items: center; /* Центруємо колонки по горизонталі */
    }

    /* Текстова колонка */
    .hero-banner-{{ section.id }} .hero-banner__text-col {
      width: 100%;
      text-align: center;
    }

    /* Колонка з картинкою — тепер працює через повзунок */
    .hero-banner-{{ section.id }} .hero-banner__image-col {
      width: {{ section.settings.image_width_mobile }}px; 
      max-width: 100%;
      margin: 0 auto; 
      flex: 0 0 auto; 
      padding: 0; /* Прибрано жорсткий padding, щоб не заважав розміру */
    }

    .hero-banner-{{ section.id }} .hero-banner__image img {
      width: 100%; 
      height: auto;
      display: block;
    }

    /* Маргінали для тексту */
    .hero-banner-{{ section.id }} .hero-banner__title {
      margin-bottom: {{ section.settings.title_margin_bottom_mobile }}px;
    }

    .hero-banner-{{ section.id }} .hero-banner__subtitle {
      margin-bottom: {{ section.settings.subtitle_margin_bottom_mobile }}px;
      font-size: {{ section.settings.subtitle_size_mobile }}px;
    }

    .hero-banner-{{ section.id }} .hero-banner__description {
      margin-bottom: {{ section.settings.description_margin_bottom_mobile }}px;
    }

    /* Декоративні елементи */
    {%- if section.settings.enable_subtitle_decor -%}
    #shopify-section-{{ section.id }} .hero-banner__subtitle::before {
      height: 12px;
      bottom: 21px;
    }

    #shopify-section-{{ section.id }} .hero-banner__subtitle::after {
      width: 0.55em;
      height: 0.55em;
      left: -5.6rem;
      top: -3.4rem;
    }
    {%- endif -%}
  }

.hero-banner-{{ section.id }} .hero-banner__subtitle strong {
  color: {{ section.settings.decoration_color }};
}

.hero-banner-{{ section.id }} .hero-banner__title strong {
  color: {{ section.settings.decoration_color }};
}

  /* Decorative elements */
  .hero-banner-{{ section.id }} {
    position: relative;
    overflow: hidden;
    isolation: isolate;
  }

  {%- for block in section.blocks -%}
    {%- if block.type == 'decor' -%}
      {%- assign safe_id = block.id | replace: '-', '_' -%}
      .hero-decor--{{ safe_id }} {
        position: absolute;
        left: {{ block.settings.pos_x }}%;
        top: {{ block.settings.pos_y }}%;
        width: {{ block.settings.size }}px;
        height: {{ block.settings.size }}px;
        transform: rotate({{ block.settings.rotation }}deg);
        opacity: {{ block.settings.opacity | divided_by: 100.0 }};
        pointer-events: none;
        z-index: 0;
      }
      .hero-decor--{{ safe_id }} img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        display: block;
      }
    {%- endif -%}
  {%- endfor -%}

  {%- for block in section.blocks -%}
    {%- if block.type == 'decor_large' -%}
      {%- assign safe_id = block.id | replace: '-', '_' -%}
      .hero-decor-lg--{{ safe_id }} {
        position: absolute;
        left: {{ block.settings.pos_x }}%;
        top: {{ block.settings.pos_y }}%;
        width: {{ block.settings.size }}px;
        height: {{ block.settings.size }}px;
        transform: rotate({{ block.settings.rotation }}deg);
        opacity: {{ block.settings.opacity | divided_by: 100.0 }};
        pointer-events: none;
        z-index: 0;
      }
      .hero-decor-lg--{{ safe_id }} img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        display: block;
      }
    {%- endif -%}
  {%- endfor -%}

{%- endstyle -%}

<div class="hero-banner-{{ section.id }}">

  {%- comment -%} Decorative elements {%- endcomment -%}
  {%- for block in section.blocks -%}
    {%- if block.type == 'decor' -%}
      {%- assign safe_id = block.id | replace: '-', '_' -%}
      <div class="hero-decor--{{ safe_id }}" {{ block.shopify_attributes }} aria-hidden="true">
        {%- if block.settings.image != blank -%}
          {{ block.settings.image | image_url: width: 200 | image_tag: loading: 'lazy', alt: '' }}
        {%- endif -%}
      </div>
    {%- elsif block.type == 'decor_large' -%}
      {%- assign safe_id = block.id | replace: '-', '_' -%}
      <div class="hero-decor-lg--{{ safe_id }}" {{ block.shopify_attributes }} aria-hidden="true">
        {%- if block.settings.image != blank -%}
          {{ block.settings.image | image_url: width: 1600 | image_tag: loading: 'lazy', alt: '' }}
        {%- endif -%}
      </div>
    {%- endif -%}
  {%- endfor -%}

  <div class="hero-banner__container" style="position:relative;z-index:2;">
    <div class="hero-banner__grid page-width">

      {%- comment -%} Text Column {%- endcomment -%}
      <div class="hero-banner__text-col">
        {%- if section.settings.title != blank -%}
          <div class="hero-banner__title">
            {{ section.settings.title }}
          </div>
        {%- endif -%}

        {%- if section.settings.subtitle != blank -%}
          <div class="hero-banner__subtitle">
            {{ section.settings.subtitle }} 
          </div>
        {%- endif -%}

        {%- if section.settings.description != blank -%}
          <div class="hero-banner__description">
            {{ section.settings.description }}
          </div>
        {%- endif -%}

        {%- if section.settings.button_label != blank -%}
          <a
            href="{{ section.settings.button_link | default: '#' }}"
            class="hero-banner__button"
            {% if section.settings.button_open_new_tab %}target="_blank" rel="noopener noreferrer"{% endif %}
          >
            {{ section.settings.button_label }}
          </a>
        {%- endif -%}
      </div>

      {%- comment -%} Image Column {%- endcomment -%}
      <div class="hero-banner__image-col">
        {%- liquid
          assign layout = section.settings.gallery_layout
          assign imgs = ''
          assign img1 = section.settings.image
          assign img2 = section.settings.image_2
          assign img3 = section.settings.image_3
          assign img4 = section.settings.image_4
          assign img_sizes = '(max-width: 749px) ' | append: section.settings.image_width_mobile | append: 'px, ' | append: section.settings.image_width | append: 'px'

          if layout == '1'
            assign visible_count = 1
          elsif layout == '2-vertical'
            assign visible_count = 2
          elsif layout == '3-top1' or layout == '3-top2'
            assign visible_count = 3
          elsif layout == '4-grid'
            assign visible_count = 4
          endif
        -%}

        {%- comment -%} Build images array {%- endcomment -%}
        {%- assign gallery_images = '' | split: '' -%}
        {%- if img1 != blank -%}{%- assign gallery_images = gallery_images | push: img1 -%}{%- endif -%}
        {%- if img2 != blank and visible_count >= 2 -%}{%- assign gallery_images = gallery_images | push: img2 -%}{%- endif -%}
        {%- if img3 != blank and visible_count >= 3 -%}{%- assign gallery_images = gallery_images | push: img3 -%}{%- endif -%}
        {%- if img4 != blank and visible_count >= 4 -%}{%- assign gallery_images = gallery_images | push: img4 -%}{%- endif -%}

        <div class="hero-banner__gallery">
          {%- if layout == '1' -%}
            {%- comment -%} Single image — keep original wrapper for border compatibility {%- endcomment -%}
            <div class="hero-gallery__item hero-gallery__item--1">
              {%- if img1 != blank -%}
                {{ img1 | image_url: width: 1200 | image_tag: loading: 'lazy', widths: '300, 400, 500, 600, 800, 1000, 1200', sizes: img_sizes, alt: img1.alt | escape }}
              {%- else -%}
                {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
              {%- endif -%}
            </div>
          {%- else -%}
            {%- for i in (1..visible_count) -%}
              {%- assign cur_img = nil -%}
              {%- if i == 1 -%}{%- assign cur_img = img1 -%}
              {%- elsif i == 2 -%}{%- assign cur_img = img2 -%}
              {%- elsif i == 3 -%}{%- assign cur_img = img3 -%}
              {%- elsif i == 4 -%}{%- assign cur_img = img4 -%}
              {%- endif -%}
              <div class="hero-gallery__item hero-gallery__item--{{ i }}">
                {%- if cur_img != blank -%}
                  {{ cur_img | image_url: width: 1200 | image_tag: loading: 'lazy', widths: '300, 400, 500, 600, 800, 1000, 1200', sizes: img_sizes, alt: cur_img.alt | escape }}
                {%- else -%}
                  {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                {%- endif -%}
              </div>
            {%- endfor -%}
          {%- endif -%}
        </div>

      </div>

    </div>
  </div>
</div>

{% schema %}
{
  "name": "Hero Banner",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Color Scheme"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "image_position",
      "label": "Image position",
      "options": [
        { "value": "right", "label": "Text left, Image right" },
        { "value": "left",  "label": "Image left, Text right" }
      ],
      "default": "right"
    },
    {
      "type": "range",
      "id": "columns_gap",
      "label": "Gap between columns (desktop)",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "columns_gap_mobile",
      "label": "Gap between columns (mobile)",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "header",
      "content": "Section Padding (Desktop)"
    },
    {
      "type": "range",
      "id": "section_padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 60
    },
    {
      "type": "range",
      "id": "section_padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 60
    },
    {
      "type": "range",
      "id": "section_padding_horizontal",
      "label": "Padding horizontal",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "header",
      "content": "Section Padding (Mobile)"
    },
    {
      "type": "range",
      "id": "section_padding_top_mobile",
      "label": "Padding top (mobile)",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "section_padding_bottom_mobile",
      "label": "Padding bottom (mobile)",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "section_padding_horizontal_mobile",
      "label": "Padding horizontal (mobile)",
      "min": 0,
      "max": 40,
      "step": 4,
      "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Title"
    },
    {
      "type": "richtext",
      "id": "title",
      "label": "Title",
      "default": "<p><strong>Turn Your Pet's Photo Into</strong></p><p><strong>A Work Of Art!</strong></p>"
    },
    {
      "type": "range",
      "id": "title_margin_bottom",
      "label": "Title margin bottom (desktop)",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "title_margin_bottom_mobile",
      "label": "Title margin bottom (mobile)",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "header",
      "content": "Subtitle"
    },
    {
      "type": "checkbox",
      "id": "enable_subtitle_decor",
      "label": "Enable subtitle decorators (curve + paw)",
      "default": true
    },
    {
      "type": "richtext",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "<p>One-Of-A-Kind Portraits, Apparel & Gifts</p>"
    },
    {
      "type": "range",
      "id": "subtitle_margin_bottom",
      "label": "Subtitle margin bottom (desktop)",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "subtitle_margin_bottom_mobile",
      "label": "Subtitle margin bottom (mobile)",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 8
    },
    {
      "type": "range",
      "id": "subtitle_size",
      "label": "Subtitle font size (desktop)",
      "min": 12,
      "max": 60,
      "step": 1,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "subtitle_size_mobile",
      "label": "Subtitle font size (mobile)",
      "min": 12,
      "max": 40,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Description"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description",
      "default": "<p>Turn any pet photo into a <strong>One-Of-A-Kind</strong> Portrait, Apparel, Or Gift — Professionally Designed And Perfected With Unlimited Revisions</p>"
    },
    {
      "type": "range",
      "id": "description_margin_bottom",
      "label": "Description margin bottom (desktop)",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 28
    },
    {
      "type": "range",
      "id": "description_margin_bottom_mobile",
      "label": "Description margin bottom (mobile)",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 20
    },
    {
      "type": "header",
      "content": "Button"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "checkbox",
      "id": "button_open_new_tab",
      "label": "Open link in new tab",
      "default": false
    },
    {
      "type": "color",
      "id": "button_bg_color",
      "label": "Button background color",
      "default": "#C89A2A"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text color",
      "default": "#FFFFFF"
    },
    {
      "type": "range",
      "id": "button_padding_vertical",
      "label": "Button padding vertical",
      "min": 4,
      "max": 32,
      "step": 2,
      "unit": "px",
      "default": 14
    },
    {
      "type": "range",
      "id": "button_padding_horizontal",
      "label": "Button padding horizontal",
      "min": 8,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "button_border_radius",
      "label": "Button border radius",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 4
    },
    {
      "type": "header",
      "content": "Image gallery"
    },
    {
      "type": "select",
      "id": "gallery_layout",
      "label": "Gallery layout",
      "options": [
        { "value": "1",        "label": "1 image" },
        { "value": "2-vertical", "label": "2 images — stacked" },
        { "value": "3-top1",   "label": "3 images — 1 top, 2 bottom" },
        { "value": "3-top2",   "label": "3 images — 2 top, 1 bottom" },
        { "value": "4-grid",   "label": "4 images — 2×2 grid" }
      ],
      "default": "1"
    },
    {
      "type": "range",
      "id": "gallery_gap",
      "label": "Gap between images",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "default": 8
    },
    {
      "type": "range",
      "id": "gallery_row1_height",
      "label": "Row 1 height",
      "min": 100,
      "max": 600,
      "step": 10,
      "unit": "px",
      "default": 280
    },
    {
      "type": "range",
      "id": "gallery_row2_height",
      "label": "Row 2 height",
      "min": 100,
      "max": 600,
      "step": 10,
      "unit": "px",
      "default": 220
    },
    {
      "type": "header",
      "content": "Image 1"
    },
    {
      "type": "header",
      "content": "Image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "range",
      "id": "image_width",
      "label": "Image width",
      "info": "Height scales proportionally. On mobile the image always fills full width.",
      "min": 100,
      "max": 900,
      "step": 10,
      "unit": "px",
      "default": 500
    },
    {
  "type": "range",
  "id": "image_width_mobile",
  "label": "Image width (mobile)",
  "min": 100,
  "max": 500,
  "step": 10,
  "unit": "px",
  "default": 300
},
    {
      "type": "checkbox",
      "id": "image_border_radius",
      "label": "Enable image border radius",
      "default": true
    },
    {
      "type": "range",
      "id": "image_border_radius_px",
      "label": "Image border radius",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "header",
      "content": "Border Decoration"
    },
    {
      "type": "checkbox",
      "id": "enable_image_border",
      "label": "Show border frame around image",
      "default": true
    },
    {
      "type": "color",
      "id": "decoration_color",
      "label": "Border color",
      "default": "#C89A2A"
    },
    {
      "type": "range",
      "id": "border_thickness",
      "label": "Border thickness",
      "min": 1,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 3
    },
    {
      "type": "range",
      "id": "decoration_border_radius",
      "label": "Border radius",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "header",
      "content": "Image 2"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "Image 2"
    },
    {
      "type": "header",
      "content": "Image 3"
    },
    {
      "type": "image_picker",
      "id": "image_3",
      "label": "Image 3"
    },
    {
      "type": "header",
      "content": "Image 4"
    },
    {
      "type": "image_picker",
      "id": "image_4",
      "label": "Image 4"
    }
  ],
  "blocks": [
    {
      "type": "decor",
      "name": "Decorative element",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image (SVG or PNG)"
        },
        {
          "type": "range",
          "id": "size",
          "label": "Size",
          "min": 20,
          "max": 520,
          "step": 5,
          "unit": "px",
          "default": 60
        },
        {
          "type": "range",
          "id": "pos_x",
          "label": "Horizontal position",
          "info": "0% = left edge, 100% = right edge",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "default": 50
        },
        {
          "type": "range",
          "id": "pos_y",
          "label": "Vertical position",
          "info": "0% = top, 100% = bottom",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "default": 50
        },
        {
          "type": "range",
          "id": "rotation",
          "label": "Rotation",
          "min": 0,
          "max": 360,
          "step": 5,
          "unit": "°",
          "default": 0
        },
        {
          "type": "range",
          "id": "opacity",
          "label": "Opacity",
          "min": 10,
          "max": 100,
          "step": 5,
          "unit": "%",
          "default": 50
        }
      ]
    }
    ,
    {
      "type": "decor_large",
      "name": "Decor element (large)",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image (SVG or PNG)"
        },
        {
          "type": "range",
          "id": "size",
          "label": "Size",
          "min": 100,
          "max": 1200,
          "step": 11,
          "unit": "px",
          "default": 595
        },
        {
          "type": "range",
          "id": "pos_x",
          "label": "Horizontal position",
          "info": "0% = left, 100% = right. Can be negative to go off-screen.",
          "min": -100,
          "max": 150,
          "step": 5,
          "unit": "%",
          "default": 0
        },
        {
          "type": "range",
          "id": "pos_y",
          "label": "Vertical position",
          "info": "0% = top, 100% = bottom.",
          "min": -100,
          "max": 150,
          "step": 5,
          "unit": "%",
          "default": 0
        },
        {
          "type": "range",
          "id": "rotation",
          "label": "Rotation",
          "min": 0,
          "max": 360,
          "step": 5,
          "unit": "°",
          "default": 0
        },
        {
          "type": "range",
          "id": "opacity",
          "label": "Opacity",
          "min": 10,
          "max": 100,
          "step": 5,
          "unit": "%",
          "default": 20
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hero Banner"
    }
  ]
}
{% endschema %}